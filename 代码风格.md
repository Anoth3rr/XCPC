# 模板代码约定

## 默认编译环境

完整版与极简版均只提供以下三行宏作为默认上下文；其他模板片段默认接在其中任一版本之后使用。完整版内联完整环境，并仅在本地包含 `linyue.hpp` 调试辅助文件；极简版直接展开必要内容。

```cpp
#define int long long
#define pii array<int, 2>
#define endl "\n"
```

因此主函数写 `signed main()`。除非题目会因此超时或超空间，所有普通整数、下标、状态、计数、权值和答案均优先写 `int`；这里的 `int` 默认就是宏展开后的 64 位有符号整数。

开局头文件不预置任何短类型别名。模板确实需要长双精度浮点时局部声明 `using ld = long double;`；确实需要固定宽度、自然溢出或 128 位中间量时，按需局部声明 `i32 / i64 / u32 / u64 / i128 / u128`。除别名声明右侧外，不直接书写 `int32_t / int64_t / uint32_t / uint64_t / int128_t / uint128_t` 等长类型名。

## 命名

- 模板、类型和命名空间名最多 10 个字符；函数名最多 7 个字符；字段、参数、局部变量和模板参数最多 3 个字符。
- 普通变量优先使用 `i, j, k, l, r, n, m, x, y, z, u, v, w, a, b, c, d, p, q, s, t, f, g`；答案使用 `ans` 或 `res`。
- 不使用下划线式变量名。短生命周期临时量可直接用 `_`；需要一一对应时只使用 `x` 与 `_x`，例如旧值与新值的对应副本。
- 同族模板统一类型名和接口名，例如普通、回滚、权值并查集均使用 `DSU`。一般查询使用 `ask`，目标明确时使用 `askMax / askMin / askGCD`；区间更新使用 `update`，单点更新使用 `modify`。
- 算法入口仍可使用 `solve`，插入、删除、合并等操作使用短且通行的 `add / erase / merge`。只有模板或类型名为保持赛时检索性可以接近 10 个字符。
- 默认 64 位整数直接使用 `int`，不再用 `ll` 重复表达同一类型。其他别名只在当前模板实际用到时声明，未使用的别名不得附带。
- 不为 `.size()` 结果书写 `(int)`、函数式转换或 `static_cast<int>`；只有省略转换会直接编译失败时才保留。反向遍历优先写不会依赖空容器下溢的循环形式。
- `size_t` 只保留在哈希函数返回值等标准库接口明确要求的签名中；普通下标、长度与循环变量仍写 `int`。

## 模板化

- 类型不同但算法相同：使用 `template<class T>`；模意义计算使用 `template<class Z>`，由 `Z=MInt<P>` 自动完成加减乘除取模。
- 编译期常量不同：使用值模板参数，如 `NTT<P, G>`。
- 同一算法的可选场景：使用布尔模板开关，而不是复制两份主体。例：`Manacher<E, O>` 分别控制偶、奇回文半径。

## 性能与定宽例外

代码的简洁、美观和统一性优先于小幅时空常数。模板不会仅因为“32 位也够用”就把普通下标改成定宽类型；字符串、图论、网络流、DP 和常规数据结构均默认使用 `int`。若该选择可能在极限数据下放大内存或缓存开销，模板只在注释中指出可将纯下标、状态或计数局部改为 `i32`，不默认牺牲代码风格。

只有以下语义或性能需求保留定宽别名：

- `MInt` 的内部值使用 `i32`，避免大规模模整数数组直接翻倍。
- 极致优化高精度版本使用 `i32 / u32` 保存 FFT 下标和十进制块；常规版本仍优先使用 `int`。
- 位块、哈希自然溢出、完整 64 位无符号模运算使用 `u64`；确需按 (2^{32}) 自然溢出时使用 `u32`。
- 乘法、叉积、平方和等可能溢出 64 位的中间量使用 `i128 / u128`。

若默认 `int` 已经实际造成超时或超空间，再只修改最热、最大的数组字段；答案、权值和容量仍保持 `int` 或原有类型模板参数。

模意义模板的选择、动态模数限制和原始 64 位模数版本的保留理由见 [数学模板索引](06%20-%20数学/README.md)。
